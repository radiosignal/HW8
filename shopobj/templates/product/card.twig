<h2>Товар</h2>

<div>
    <h3>{{ product.name }}</h3>
    <img src="../img/{{ product.image }}" alt="img" width="150"><br>
    <p>{{ product.description }}</p>
    <p>price: {{ product.price }}</p>

</div>